---
description: 启动桃李报告时让开发服务脱离终端，避免浏览器连接被重置
alwaysApply: true
---

# 开发服务不要绑在终端上

浏览器打开 `http://localhost:5181/` 出现 Connection Failed / `ERR_CONNECTION_RESET`，是因为 Vite 跟着启动它的终端一起退出了，端口上已经没有进程。

启动报告只用：

```
npm run dev:keep
```

不要用会随对话或终端结束被关掉的 `npm run dev`。`dev:keep` 把进程交给系统独立运行；端口已经在响应时直接复用，不要再起第二个。
