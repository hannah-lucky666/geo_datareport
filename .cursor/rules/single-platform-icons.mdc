---
description: 词条明细按单平台取数，监测平台只显示当前平台图标
alwaysApply: true
---

# 词条明细只显示当前平台

词条数据明细每一页只对应一个平台（DeepSeek / 豆包 / 文心 / Kimi）。提及率、top1提及率、截图都是该平台的单日数据。

- 采集必须传 `platform_ids`（复数）。不要传单数 `platform_id`，否则接口会退化成全平台汇总。
- 「监测平台」列只渲染当前页这一个平台的 icon。不要把四个平台 logo 叠在一起，也不要靠透明度或描边去“标出”当前平台。
