---
description: GEO 报告禁止出现「位次」
alwaysApply: true
---

# 禁止在报告中出现「位次」

整份 GEO 数据报告的页面、表头、总结文案都不要出现「位次」「平均位次」「平均提及位次」「提及位次」。数据系统词条明细用的是 top1 提及率。

- 词条明细表对应列名固定为「top1提及率」，取值用接口字段 `entries.top1_mention_rate`，展示成百分数（如 `100.0%`）。
- 不要再把 `position` / `avg_position` 渲染到任何页面上。
- 采集脚本可以继续接收这些字段做核对，但写入页面数据时只保留 `top1_mention_rate`，对外文案一律不提位次。
